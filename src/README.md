# Instructions for Running the Code

These instructions are for Linux environments. The commands work if you are executing them from the root directory of the project.

### Generetaing input data
To generate input data, run the following command in the terminal:

```bash
python3 src/generate_input.py
```