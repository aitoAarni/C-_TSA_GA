{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug TSP (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/src/tsp_solver",
            "args": [],
            "cwd": "${workspaceFolder}/src"
        }
    ]
}
